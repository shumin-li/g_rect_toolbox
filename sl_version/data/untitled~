%% demo coastline

lonlim = [-124.5 -122.8];
latlim = [48.8 49.5];
outside_index = ncst(:,1) < lonlim(1) | ncst(:,1) > lonlim(2) | ...
    ncst(:,2) < latlim(1) | ncst(:,2) > latlim(2);

ncst(outside_index,:) = [];

% save('coastline_demo.mat','ncst');

